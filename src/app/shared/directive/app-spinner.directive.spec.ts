import { AppSpinnerDirective } from './app-spinner.directive';

describe('AppSpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = new AppSpinnerDirective();
    expect(directive).toBeTruthy();
  });
});
